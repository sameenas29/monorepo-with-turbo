{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "benchmark": "node benchmark.js",
    "simpleBuild": "yarn workspaces foreach run build"
  },
  "devDependencies": {
    "lint-staged": "^13.0.1",
    "prettier": "^2.5.0",
    "simple-git-hooks": "^2.7.0",
    "nx": "14.2.1",
    "lerna": "5.1.0",
    "lage": "1.3.1",
    "turbo": "1.2.11",
    "typescript": "4.5.2"
  }
}
